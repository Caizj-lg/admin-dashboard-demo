# 项目结构详解

## 📁 完整目录树

```
QuantOps 量化后台/
│
├── 📄 App.tsx                                    # 应用主入口，路由管理
├── 📄 README.md                                  # 项目说明文档
│
├── 📂 types/                                     # 类型定义目录 ⭐
│   ├── index.ts                                  # 通用类型（PageType、分页等）
│   ├── market-data.ts                            # 市场数据类型定义
│   ├── transaction-data.ts                       # 成交数据类型定义
│   └── long-term-tags.ts                         # 长期标签类型定义
│
├── 📂 constants/                                 # 常量配置目录 ⭐
│   ├── navigation.ts                             # 导航配置（菜单项、应用配置）
│   └── table-config.ts                           # 表格配置（列定义、分页配置）
│
├── 📂 services/                                  # API服务层 ⭐
│   ├── api.ts                                    # API基础配置（请求封装）
│   └── market-data.service.ts                    # 市场数据服务
│
├── 📂 hooks/                                     # 自定义Hooks ⭐
│   └── usePagination.ts                          # 分页逻辑Hook
│
├── 📂 lib/                                       # 工具函数库 ⭐
│   └── formatters.ts                             # 格式化工具（价格、金额等）
│
├── 📂 mocks/                                     # Mock数据 ⭐
│   ├── market-data.mock.ts                       # 市场数据Mock
│   ├── transaction-data.mock.ts                  # 成交数据Mock
│   └── long-term-tags.mock.ts                    # 长期标签Mock
│
├── 📂 components/                                # 组件目录
│   │
│   ├── 📂 layout/                                # 布局组件 ⭐
│   │   ├── MainLayout.tsx                        # 主布局容器
│   │   ├── Sidebar.tsx                           # 侧边栏导航
│   │   └── Header.tsx                            # 顶部栏
│   │
│   ├── 📂 common/                                # 通用业务组件 ⭐
│   │   ├── PageHeader.tsx                        # 页面标题组件
│   │   └── DataTablePagination.tsx               # 数据表格分页组件
│   │
│   ├── 📂 pages/                                 # 页面组件 ⭐
│   │   ├── DashboardPage.tsx                     # 仪表盘页面
│   │   ├── MarketDataPage.tsx                    # 市场数据页面
│   │   ├── TransactionDataPage.tsx               # 成交数据页面
│   │   ├── LongTermTagsPage.tsx                  # 长期标签页面
│   │   └── SettingsPage.tsx                      # 设置页面
│   │
│   ├── 📂 ui/                                    # 基础UI组件库
│   │   ├── button.tsx                            # 按钮组件
│   │   ├── input.tsx                             # 输入框组件
│   │   ├── table.tsx                             # 表格组件
│   │   ├── card.tsx                              # 卡片组件
│   │   ├── select.tsx                            # 选择器组件
│   │   ├── dropdown-menu.tsx                     # 下拉菜单组件
│   │   ├── avatar.tsx                            # 头像组件
│   │   └── ...                                   # 其他UI组件
│   │
│   └── 📂 figma/                                 # Figma相关组件
│       └── ImageWithFallback.tsx                 # 图片组件（受保护）
│
├── 📂 docs/                                      # 文档目录 ⭐
│   ├── ARCHITECTURE.md                           # 架构设计文档
│   ├── DEVELOPMENT_GUIDE.md                      # 开发指南
│   └── PROJECT_STRUCTURE.md                      # 项目结构详解（本文档）
│
├── 📂 styles/                                    # 样式目录
│   └── globals.css                               # 全局样式和Tailwind配置
│
└── 📂 guidelines/                                # 指南目录
    └── Guidelines.md                             # 项目指南

⭐ 标记为核心目录/文件
```

## 🎯 各目录职责详解

### 1. `/types` - 类型定义层

**职责：** 集中管理所有 TypeScript 类型定义

**文件说明：**
- `index.ts` - 通用类型（路由、分页、API响应等）
- `market-data.ts` - 市场数据相关类型
- `transaction-data.ts` - 成交数据相关类型
- `long-term-tags.ts` - 长期标签相关类型

**设计原则：**
- 所有数据结构必须有明确的类型定义
- 使用 `interface` 定义对象类型
- 使用 `type` 定义联合类型
- 复用基础类型，避免重复定义

**示例：**
```typescript
// types/market-data.ts
export interface MarketData {
  id: number;
  thscode: string;
  tradeDate: string;
  // ...
}
```

---

### 2. `/constants` - 常量配置层

**职责：** 统一管理应用中的常量和配置

**文件说明：**
- `navigation.ts` - 导航菜单配置、应用基础配置
- `table-config.ts` - 表格列配置、分页配置

**设计原则：**
- 避免硬编码，所有常量集中管理
- 使用大写蛇形命名（UPPER_SNAKE_CASE）
- 配置应该是只读的

**示例：**
```typescript
// constants/navigation.ts
export const APP_CONFIG = {
  name: 'QuantOps 量化后台',
  logo: 'Q',
  defaultPage: 'market-data',
};
```

---

### 3. `/services` - API服务层

**职责：** 封装所有API调用逻辑

**文件说明：**
- `api.ts` - API基础配置和通用请求函数
- `market-data.service.ts` - 市场数据API服务
- 未来可扩展：`transaction-data.service.ts`、`long-term-tags.service.ts`

**设计原则：**
- 统一的请求/响应处理
- 错误处理和日志记录
- Mock数据和真实API的无缝切换

**示例：**
```typescript
// services/market-data.service.ts
export async function getMarketDataList(filter, page, pageSize) {
  // 当前使用Mock，未来替换为真实API
  return await apiGet(API_ENDPOINTS.MARKET_DATA);
}
```

---

### 4. `/hooks` - 自定义Hooks层

**职责：** 提取可复用的业务逻辑

**文件说明：**
- `usePagination.ts` - 分页逻辑Hook
- 未来可扩展：`useFilter.ts`、`useSort.ts`、`useMarketData.ts`

**设计原则：**
- Hook名称必须以 `use` 开头
- 单一职责，逻辑清晰
- 返回一致的接口结构

**示例：**
```typescript
// hooks/usePagination.ts
export function usePagination(data, defaultPageSize) {
  // 返回分页相关的状态和方法
  return {
    currentPage,
    currentData,
    goToPage,
    // ...
  };
}
```

---

### 5. `/lib` - 工具函数库

**职责：** 提供纯函数工具集

**文件说明：**
- `formatters.ts` - 格式化工具（价格、金额、百分比等）
- 未来可扩展：`validators.ts`、`calculators.ts`

**设计原则：**
- 必须是纯函数，无副作用
- 函数命名清晰，功能单一
- 充分的文档注释

**示例：**
```typescript
// lib/formatters.ts
export function formatPrice(value: number): string {
  return value.toFixed(2);
}
```

---

### 6. `/mocks` - Mock数据层

**职责：** 提供开发和测试用的模拟数据

**文件说明：**
- `market-data.mock.ts` - 市场数据Mock
- `transaction-data.mock.ts` - 成交数据Mock
- `long-term-tags.mock.ts` - 长期标签Mock

**设计原则：**
- 数据结构必须符合类型定义
- 数据要真实、完整、有代表性
- 便于替换为真实API

**示例：**
```typescript
// mocks/market-data.mock.ts
export const MOCK_MARKET_DATA: MarketData[] = [
  {
    id: 1,
    thscode: '000001.SZ',
    // ... 完整的字段
  },
];
```

---

### 7. `/components/layout` - 布局组件

**职责：** 提供应用的整体布局结构

**文件说明：**
- `MainLayout.tsx` - 主布局容器（组合Sidebar和Header）
- `Sidebar.tsx` - 侧边栏导航
- `Header.tsx` - 顶部栏（搜索、通知、用户菜单）

**设计原则：**
- 布局组件只负责结构，不包含业务逻辑
- 响应式设计
- 可配置和可扩展

---

### 8. `/components/common` - 通用业务组件

**职责：** 跨页面可复用的业务组件

**文件说明：**
- `PageHeader.tsx` - 页面标题组件
- `DataTablePagination.tsx` - 数据表格分页组件

**设计原则：**
- 高度可复用
- Props接口清晰
- 样式可定制

---

### 9. `/components/pages` - 页面组件

**职责：** 实现具体的页面功能

**文件说明：**
- `DashboardPage.tsx` - 仪表盘
- `MarketDataPage.tsx` - 市场数据页
- `TransactionDataPage.tsx` - 成交数据页
- `LongTermTagsPage.tsx` - 长期标签页
- `SettingsPage.tsx` - 设置页

**设计原则：**
- 组合使用 layout、common、ui 组件
- 使用自定义 Hooks 处理业务逻辑
- 通过 services 获取数据

---

### 10. `/components/ui` - 基础UI组件库

**职责：** 提供无业务逻辑的纯UI组件

**说明：** 这些是基础组件库，已经预先配置好，包含：
- 表单组件（button、input、select等）
- 布局组件（card、dialog等）
- 反馈组件（alert、toast等）
- 数据展示（table、badge等）

**使用原则：**
- 直接导入使用，无需修改
- 通过 props 定制样式和行为
- 遵循组件文档使用

---

## 🔄 数据流图

```
┌─────────────┐
│  Mock Data  │
│  /mocks/    │
└──────┬──────┘
       │
       ▼
┌──────────────┐
│   Services   │  ◄── 未来替换为真实API
│  /services/  │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│  Custom      │
│  Hooks       │
│  /hooks/     │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│  Page        │
│  Components  │
│  /pages/     │
└──────┬───────┘
       │
       ├──────► Common Components
       │        /common/
       │
       ├──────► Layout Components
       │        /layout/
       │
       └──────► UI Components
                /ui/
```

## 🎨 组件层次图

```
App.tsx
  └── MainLayout
       ├── Sidebar (导航菜单)
       ├── Header (搜索、通知、用户)
       └── Page Component
            ├── PageHeader (页面标题、操作按钮)
            ├── Filter Card (筛选表单)
            ├── Data Table Card
            │    ├── Table (数据展示)
            │    └── DataTablePagination
            └── Other Components
```

## 📋 文件关系示例

以 **市场数据页面** 为例，展示文件之间的依赖关系：

```
MarketDataPage.tsx (页面组件)
  │
  ├── imports from types/market-data.ts (类型定义)
  ├── imports from mocks/market-data.mock.ts (Mock数据)
  ├── imports from hooks/usePagination.ts (分页逻辑)
  ├── imports from lib/formatters.ts (格式化工具)
  ├── imports from components/common/PageHeader.tsx (页面标题)
  ├── imports from components/common/DataTablePagination.tsx (分页组件)
  └── imports from components/ui/* (基础UI组件)
```

## 🚀 扩展指南

### 添加新的数据模块

假设要添加"策略管理"模块：

1. **创建类型定义**
   ```
   /types/strategy.ts
   ```

2. **创建Mock数据**
   ```
   /mocks/strategy.mock.ts
   ```

3. **创建API服务**
   ```
   /services/strategy.service.ts
   ```

4. **创建页面组件**
   ```
   /components/pages/StrategyPage.tsx
   ```

5. **添加导航配置**
   ```
   /constants/navigation.ts (添加导航项)
   ```

6. **注册路由**
   ```
   /App.tsx (添加路由逻辑)
   ```

---

## 📌 重要提示

### 核心原则
1. **分层清晰** - 每一层都有明确的职责
2. **单向依赖** - 下层不依赖上层
3. **类型安全** - 所有数据都有类型定义
4. **可复用性** - 公共逻辑提取为Hooks和工具函数
5. **可维护性** - 代码结构清晰，易于理解和修改

### 受保护的文件
以下文件由系统管理，不应修改：
- `/components/figma/ImageWithFallback.tsx`

### 配置文件
- `/styles/globals.css` - 全局样式配置，包含Tailwind设置

---

## 📞 获取帮助

- 架构设计问题：查看 `/docs/ARCHITECTURE.md`
- 开发规范问题：查看 `/docs/DEVELOPMENT_GUIDE.md`
- 项目说明：查看 `/README.md`
